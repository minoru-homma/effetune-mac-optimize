[package]
name = "fifteen_band_peq"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
peq_dsp = { path = "../peq-dsp" }

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
